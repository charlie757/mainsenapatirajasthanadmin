import 'package:flutter/material.dart';

class PiChartModel {
  final String x;
  final int y;
  final mapper;
  final Color color;
  PiChartModel(this.x, this.y, this.mapper, this.color);
}
